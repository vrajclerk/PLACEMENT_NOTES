CREATE DATABASE ecomm;
USE ecomm;

CREATE TABLE customer(
id INT,
name VARCHAR(50)
);

INSERT INTO customer(id,name)
VALUES
(1,"VRAJ"),
(2,"RAJ"),
(3,"NIRAJ"),
(4,"ANUJ"),
(5,"DARSHINI");

SELECT * FROM CUSTOMER;

CREATE TABLE Orders(
id INT PRIMARY KEY,
ordername VARCHAR(50)
);

INSERT INTO Orders(id,ordername)
VALUES
(2,"IPHONE"),
(3,"LAPTOP"),
(4,"INJECTION");

SELECT * FROM ORDERS;

SELECT * FROM 
CUSTOMER
INNER JOIN ORDERS
ON CUSTOMER.id=ORDERS.id;

-- INNER JOIN

SELECT  customer.id,orders.ordername,orders.id
FROM 
CUSTOMER
INNER JOIN ORDERS
ON CUSTOMER.id=ORDERS.id;

-- LEFT JOIN

SELECT *
FROM
CUSTOMER
LEFT JOIN ORDERS
ON CUSTOMER.id=ORDERS.id;

-- RIGHT JOIN

SELECT *
FROM
CUSTOMER
RIGHT JOIN ORDERS
ON CUSTOMER.id=ORDERS.id;

-- FULL OUTER JOIN

SELECT *
FROM
CUSTOMER
LEFT JOIN ORDERS
ON CUSTOMER.id=ORDERS.id
UNION
SELECT *
FROM
CUSTOMER
RIGHT JOIN ORDERS
ON CUSTOMER.id=ORDERS.id;

-- CROSS JOIN

SELECT * FROM 
CUSTOMER
CROSS JOIN ORDERS;

